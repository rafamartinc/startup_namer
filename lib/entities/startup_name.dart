class StartupName {
  /*  This application follows the Entity-Boundary-Interactor pattern.
      The entities, for the sake of simplicity, are also used as Request Models
      and Response Models, until complexity increases. */

  final String name;

  StartupName(this.name);
}